# Aster currently supports no environment variables.
#
# Never place an API key, provider origin, proxy, certificate override, or
# diagnostic setting in an environment file. Configure the API key only through
# the Rust-owned native prompt in Aster Settings; the desktop backend stores it
# in Windows Credential Manager.

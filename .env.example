# Aster supports no environment-based runtime configuration.
#
# Never place an API key, provider/model override, origin, proxy, certificate
# override, balance, or diagnostic setting in an environment file. Configure
# each provider credential only through the Rust-owned native Windows prompt in
# Aster Settings. The desktop backend stores every key in a separate,
# provider-scoped Windows Credential Manager target.
